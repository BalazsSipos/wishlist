@model AddEventRequest
@{
    ViewData["Action"] = "Add";
}
<h1 class="black">Add event</h1>
<form asp-controller="Event" asp-action="@ViewData["Action"]" method="post" enctype="multipart/form-data">
    <div asp-validation-summary="All" class="text-danger"></div>

    <div class="form-group">
        <select asp-for="SelectedEventTypeId">
            <option selected disabled>"Choose an event type!"</option>
            @foreach (var eventType in Model.EventTypes)
            {
                <option value="@eventType.EventTypeId">@eventType.Name</option>
            }
        </select>
    </div>

    <div class="form-group">
        <label asp-for="EventDate" class="font-weight-bold black">EventDate</label><br />
        <input asp-for="EventDate" type="date" input value="YYYY-MM-DD" min="2019-11-01" max="2025-12-31">
        <span asp-validation-for="EventDate" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Name" class="font-weight-bold black">Name</label><br />
        <input asp-for="Name">
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Address" class="font-weight-bold black">Address</label><br />
        <textarea asp-for="Address" rows="3"></textarea>
        <span asp-validation-for="Address" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Message" class="font-weight-bold black">Message</label><br />
        <input asp-for="Message">
        <span asp-validation-for="Message" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Image" class="font-weight-bold black">Choose picture</label><br />
        <center><input type="file" asp-for="Image" lang="en" accept=".jpg, .png, .jpeg|image/*"></center>
    </div>
    <button type="submit" class="btn btn-primary">Add</button>
</form>
<br />
<form asp-controller="Home" asp-action="Index" method="get">
    <button type="submit" class="btn btn-info">Cancel</button>
</form>
